# ---- Backend ----
MONGO_URL="mongodb://localhost:27017"
DB_NAME="mezbaan_restro"

# ---- Frontend ----
# Public backend URL that the mobile client will call. Must be reachable from device/emulator.
EXPO_PUBLIC_BACKEND_URL="https://your-domain.example.com"

# ---- Admin ----
# The backend currently hard-codes ADMIN_PASSCODE=MEZBAAN2026 in server.py.
# For production, move it to an env var and read via os.environ["ADMIN_PASSCODE"].
